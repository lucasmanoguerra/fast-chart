[package]
name = "fc-cache"
version = "0.1.0"
edition = "2021"
description = "Specialized caches for chart rendering performance"
license = "MIT OR Apache-2.0"

[dependencies]
# Zero external dependencies — std only
